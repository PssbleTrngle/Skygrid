package com.possible_triangle.skygrid.minigame.gamedata

import net.minecraft.core.BlockPos
import net.minecraft.world.scores.PlayerTeam

object Flags {

    fun place(team: PlayerTeam, pos: BlockPos) {
        TODO()
    }

    fun remove(team: PlayerTeam) {
        TODO()
    }

}
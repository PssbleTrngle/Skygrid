import styled from 'styled-components'

const Page = styled.section`
   display: grid;
   align-items: center;
   justify-content: center;
   min-height: 40vh;
   text-align: center;
`

export default Page